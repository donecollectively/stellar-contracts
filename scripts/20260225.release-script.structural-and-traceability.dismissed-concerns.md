# Dismissed Concerns: scripts/release — Structural Review + Traceability

**Date**: 2026-02-25
**Audit**: `20260225.release-script.structural-and-traceability.audit-result.md`

#### Dismissed: computeReleaseVersion testability
**During**: Testability awareness pass
**Location**: In `computeReleaseVersion` (computes new version from bump mode) at `scripts/release:115-140`

**Concern**: Function closes over module-level `currentVersion` and `isPreRelease` globals, making it untestable without running the full script.

**Rationale for dismissal**: This is a 300-line release script, not a library. The bump logic is well-specified in REQT-5vay51a5w9 and stable. Extracting it into a separate module would be over-engineering at this scale. If bump modes grow more complex, this becomes a real concern.
