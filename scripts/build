#!/bin/bash

#//! EXPECTS to be run in context of `pnpm exec`

rollup \
  --config rollup.config.ts \
  --configPlugin 'esbuild={tsconfig:"./tsconfig.rollupconfig.json"}' \
  $*

tsc --declaration --declarationMap -p tsconfig.dts.json 
