# Work Unit: 8f2b324ncw

**Title**: In-Flight Transaction Integration for CachedUtxoIndex
**Created**: 2026-02-27
**Source**: architecture session (in-flight tx discovery interview on utxoIndex.ARCHITECTURE.md v2.0)

> **Required context**: Load [work-planner.SKILL.md](./work-planner.SKILL.md) for lifecycle protocol, team composition, and sign-off procedures before operating on this work unit.

## Scope

**Required Skills**: offchain-coder, stellar-testing
**ARCH Items**:
- ARCH-fk1fgq7map (In-Flight Transaction Manager — extends CachedUtxoIndex)
- ARCH-yfw1d6p8hn (PendingTxEntry software object)
- ARCH-0vh64y2ap9 (Submission → Index registration interaction)
- ARCH-cp9460d99n (Index → CapoDappProvider events interaction)
- ARCH-b03wafgv72 (Register Pending Transaction dataflow)
- ARCH-pt3qaa3zm4 (UtxoIndex — parent component)

**REQT Items**:

### Phase 1 — Type & Store Foundation
- REQT/jqz2m497vx (PendingTxEntry Type)
- REQT/2e83bf33rz (Pending Tx Store Interface Methods)
- REQT/t3f4bfcr8q (Pending Tx Rollback Interface Methods)
- REQT/hf6z7ga92d (Pending Tx Purge Interface Method)
- REQT/yz1ymcenzx (PendingTxs Table Schema)
- REQT/9d83h3a7df (Pending Tx CRUD Implementation)
- REQT/5skb90kx7n (Rollback Operations Implementation)
- REQT/5799nq1d0x (Purge Implementation)

### Phase 2 — Core CachedUtxoIndex Integration
- REQT/p2ts24jbkg (Register Pending Transaction)
- REQT/c3ytg4rttd (Deadline Calculation)
- REQT/2w2yyc2m1k (In-Memory Pending Map)
- REQT/58b9nzgcbj (Confirm Pending Transaction)
- REQT/a9y19g0pmr (Rollback Expired Pending Transaction)

### Phase 3 — Query & State API
- REQT/mjhf1yezr9 (isPending Query)
- REQT/r0y7s2vggr (getPendingTxs Query)
- REQT/9r9rc1hrfv (pendingSyncState Property)
- REQT/agg98btez8 (Purge Old Pending Entries)

### Phase 4 — Events & Startup Recovery
- REQT/fz6z7rr702 (Pending Transaction Events)
- REQT/fn70x96nxm (Startup Recovery)

## Problem / Context

The CachedUtxoIndex serves as the Capo's network client (`ReadonlyCardanoClient`) but has no awareness of locally-submitted transactions. After a user submits a transaction, the index is blind to it for up to 60 seconds (next Blockfrost poll). This causes:

1. **Stale reads after writes** — queries return old state, confusing users and risking duplicate submissions
2. **Double-spend risk** — transaction builders may select UTXOs already consumed by a pending tx
3. **No pending-state visibility** — no way for UI to show "pending" indicators or defer conflicting operations

The solution connects the transaction submission path to the UTXO index: submitted transactions are decoded via existing pipelines, their inputs marked as speculatively spent and outputs indexed as pending. Each transaction has a deadline (based on chain time, not wallclock) after which unconfirmed entries are rolled back. Events notify the CapoDappProvider for ✓ confirmation toasts and recovery UI on rollback.

### Key Design Decisions (from architecture session)

- **CBOR as integration format** — submission side passes signed CBOR hex; no Helios coupling leak
- **No new fields on UtxoIndexEntry/RecordIndexEntry** — pending state is a property of the transaction (PendingTxEntry table), not the UTXO
- **Reuse `spentInTx`** for speculative spends — the pending-tx table determines if a spend is speculative vs confirmed
- **`isPending()` on CachedUtxoIndex**, not on `FoundDatumUtxo` — keeps FoundDatumUtxo as a plain type
- **Chain-time deadlines** — `txValidityEnd + graceBuffer` compared to last synced block slot time
- **10s deadline timer** — separate from 60s Blockfrost poll; also runs 72h purge
- **Register after successful submit** — avoids rollback path for submission errors
- **In-memory `Map<txHash, txd>`** for live TxDescription; session-scoped, not persisted
- **CapoDappProvider mediates** all UI access to index events

## Focus Files

### Architecture & Requirements
- `src/networkClients/UtxoIndex/utxoIndex.ARCHITECTURE.md` — architecture doc with In-Flight section (v2.0)
- `src/networkClients/UtxoIndex/utxoIndex.arch.jsonl` — structured architecture records
- `src/networkClients/UtxoIndex/UtxoIndex.reqts.md` — requirements (REQT-1.11, 2.2, 3.5, 5.3)

### Implementation Targets
- `src/networkClients/UtxoIndex/types/PendingTxEntry.ts` — new type definition (Phase 1)
- `src/networkClients/UtxoIndex/UtxoStoreGeneric.ts` — interface extensions (Phase 1)
- `src/networkClients/UtxoIndex/DexieUtxoStore.ts` — Dexie implementation + schema bump (Phase 1)
- `src/networkClients/UtxoIndex/CachedUtxoIndex.ts` — core integration (Phases 2-4)

### Context (read, not modified)
- `src/networkClients/UtxoIndex/types/UtxoIndexEntry.ts` — existing UTXO type (spentInTx field)
- `src/networkClients/UtxoIndex/types/RecordIndexEntry.ts` — existing record type
- `src/networkClients/TxSubmissionTracker.ts` — caller side (future wiring)
- `src/networkClients/BatchSubmitController.ts` — caller side (future wiring)
- `src/StellarTxnContext.ts` — TxDescription type, validity window
- `src/ui/CapoDappProvider.tsx` — downstream consumer (future wiring)
- `reference/essential-stellar-offchain.md` — off-chain patterns
- `reference/essential-cardano.md` — Cardano UTxO model, validity windows

## Plan / Guidance

### Implementation Strategy: Bottom-Up by Layer

Work proceeds bottom-up through 4 phases. Each phase produces testable, committable work. Phase boundaries are chosen so each layer is complete and independently verifiable before the next layer builds on it.

### Phase 1: Type & Store Foundation

**Goal**: PendingTxEntry type exists, store interface is extended, Dexie implementation is complete. No CachedUtxoIndex changes yet.

1. Create `types/PendingTxEntry.ts` with the type definition (Helios-free, IndexedDB-serializable)
2. Extend `UtxoStoreGeneric` interface with pending-tx methods:
   - CRUD: `savePendingTx`, `findPendingTx`, `getPendingByStatus`, `setPendingTxStatus`
   - Rollback: `clearSpentByTx`, `deleteUtxosByTxHash`, `deleteRecordsByTxHash`
   - Cleanup: `purgeOldPendingTxs`
3. Implement all methods in `DexieUtxoStore`:
   - Add `pendingTxs` table with `txHash` PK and `status` index
   - Add `spentInTx` index to utxos table (enables efficient `clearSpentByTx` rollback queries; `null` values excluded from IndexedDB indexes so cost is minimal)
   - Schema version bump (v3)
   - Implement all interface methods

**Verification**: Store methods can be unit-tested in isolation with a test database.

### Phase 2: Core CachedUtxoIndex Integration

**Goal**: Transactions can be registered, confirmed, and rolled back. The index correctly reflects pending state in its stored data.

1. Add in-memory `pendingTxMap: Map<string, TxDescription>`
2. Implement `registerPendingTx()`:
   - Decode CBOR via `decodeTx()`
   - Compute deadline (`txValidityEnd + graceBuffer` from decoded tx validity interval)
   - Persist PendingTxEntry to store (status: "pending")
   - Mark inputs: set `spentInTx` on each consumed UTXO
   - Index outputs: run through `indexUtxoFromOutput()`
   - Parse records if Capo attached
   - Store `txd` in pendingTxMap
3. Modify `checkForNewTxns()`: when a txHash matches a pending entry, skip re-indexing and call `setPendingTxStatus("confirmed")`
4. Implement `checkPendingDeadlines()` on 10s timer:
   - Query pending entries where deadline < last synced block slot time
   - For each: `clearSpentByTx`, `deleteUtxosByTxHash`, `deleteRecordsByTxHash`, `setPendingTxStatus("rolled-back")`

**Verification**: Register a pending tx, verify UTXOs/records appear in store with correct spentInTx state. Simulate confirmation by feeding the same txHash through checkForNewTxns. Simulate rollback by advancing past deadline.

### Phase 3: Query & State API

**Goal**: Consumers can check pending state and the index exposes its sync status.

1. Implement `isPending(item: TxOutputId | string | FoundDatumUtxo)` — synchronous, uses pendingTxMap
2. Implement `getPendingTxs()` — returns pending entries from store
3. Implement `pendingSyncState` getter — tracks stale/fresh lifecycle
4. Wire purge into the 10s timer — `purgeOldPendingTxs(72h)`

**Verification**: isPending returns correct results for pending-origin UTXOs, speculatively-spent UTXOs, and confirmed UTXOs.

### Phase 4: Events & Startup Recovery

**Goal**: Downstream consumers receive notifications. Page-reload recovery works.

1. Extend EventEmitter type with `txConfirmed`, `txRolledBack`, `pendingSynced`
2. Wire event emission into confirmation path (Phase 2 confirm) and rollback path (Phase 2 rollback)
3. Implement startup recovery:
   - On construction, load pending entries from Dexie
   - Set `pendingSyncState` to "stale"
   - After first sync cycle, resolve pending state and flip to "fresh"
   - Emit `pendingSynced`

**Verification**: Simulate page reload by constructing a new CachedUtxoIndex against a Dexie DB that already has pending entries. Verify events fire and state transitions correctly.

### Not In Scope (Future Work)

- Wiring `TxSubmissionTracker` to call `registerPendingTx()` — separate work unit for caller integration
- Wiring `CapoDappProvider` to subscribe to new events — separate work unit for UI integration
- Emulator/test-environment support for pending-tx features — to be assessed after implementation

## Acceptance Criteria

- [ ] `PendingTxEntry` type defined, Helios-free, IndexedDB-serializable
- [ ] `UtxoStoreGeneric` interface extended with all pending-tx methods
- [ ] `DexieUtxoStore` implements all pending-tx methods with new `pendingTxs` table
- [ ] `registerPendingTx()` correctly decodes CBOR, marks inputs spent, indexes outputs, persists PendingTxEntry
- [ ] Deadline computed from tx validity interval + grace buffer, compared to chain time
- [ ] `checkForNewTxns()` confirms pending txs when seen in blocks (skips re-indexing)
- [ ] `checkPendingDeadlines()` rolls back expired entries (restores inputs, removes pending outputs/records)
- [ ] `isPending()` returns correct results for all UTXO states
- [ ] `getPendingTxs()` returns pending entries
- [ ] `pendingSyncState` transitions from stale to fresh after first sync
- [ ] Events (`txConfirmed`, `txRolledBack`, `pendingSynced`) emitted at correct lifecycle points
- [ ] Startup recovery loads pending from Dexie, resolves state on first sync
- [ ] 72h purge runs on 10s timer
- [ ] All in-scope requirements marked IMPLEMENTED/NEEDS VERIFICATION
- [ ] Existing tests continue to pass

## Sign-Offs

### Pre-Work
| Reviewer | Status | Date | Notes / References |
|----------|--------|------|--------------------|
| Architect | Pending | — | — |
| Code Whisperer | Pending | — | — |
| Domain-Fit Tester (stellar-testing) | Pending | — | — |

### Post-Work
| Reviewer | Status | Date | Notes / References |
|----------|--------|------|--------------------|
| Architect | Pending | — | — |
| Code Whisperer | Pending | — | — |
| Domain-Fit Tester (stellar-testing) | Pending | — | — |

## Advisory Notes

### Pre-Work Collaborative Review (2026-02-28)

#### Architect leads — findings

##### Finding F1: Domain-Fit Tester domain mismatch in Sign-Offs table

The work unit's Sign-Offs table lists `Domain-Fit Tester (offchain)` but the Required Skills include both `offchain-coder` and `stellar-testing`. The domain-fit tester for this work is the Stellar Testing Expert (tessa), whose domain matches `stellar-testing` — the skill that tests stellar-contracts code. The sign-off table should reference `Domain-Fit Tester (stellar-testing)` to be precise about the domain match, since `offchain` is a coder skill, not a tester skill.

> **Resolution**: Updated both pre-work and post-work sign-off tables from `(offchain)` to `(stellar-testing)`. Stakeholder confirmed.

#### Code Whisperer — sequenced follow-up

##### Finding F2: Missing `spentInTx` index for rollback query performance

The plan calls for `clearSpentByTx(txHash)` — querying UTXOs where `spentInTx === txHash` to restore speculatively-spent inputs. The current Dexie schema has no index on `spentInTx`. Without an index, this requires a full table scan of all UTXOs. For a production index with thousands of UTXOs, this is a performance concern during rollback. The plan should account for adding a `spentInTx` index in the schema bump.

> **Resolution**: Add `spentInTx` as a separate Dexie index on the utxos table in the v3 schema bump (alongside the new `pendingTxs` table). IndexedDB excludes `null` values from indexes, so the index stays small — only spent UTXOs are indexed. No upgrade migration needed; Dexie handles adding an index to existing data transparently. Updated Plan Phase 1 guidance accordingly.

##### Finding F3: 10s deadline timer lifecycle and cleanup

The plan introduces a 10s `checkPendingDeadlines()` timer alongside the existing 60s refresh timer, but doesn't address timer lifecycle management. The existing `startPeriodicRefresh()`/`stopPeriodicRefresh()` pattern only manages the 60s timer. If the 10s timer isn't also stopped on `stopPeriodicRefresh()` (or through a parallel stop method), it will leak in tests and potentially in production when the index is torn down. The plan should specify how the deadline timer integrates with the existing timer lifecycle.

#### Stellar Testing Expert — sequenced follow-up

##### Finding F4: Test strategy gap — no emulator infrastructure for pending-tx features

The work unit's architecture doc acknowledges this open question: "Current CachedUtxoIndex tests are all Blockfrost/preprod-based. Pending-tx features need either emulator-based test infrastructure or integration tests with real submissions." The plan's verification notes assume test-level operations (e.g., "Register a pending tx, verify UTXOs/records appear") but doesn't specify HOW these tests will work. Pending-tx registration requires signed CBOR from a real or emulated submission — the existing test helpers (`copyIndexData`, `createPartialSyncIndex`) operate on synced Blockfrost data, not locally-constructed transactions. The test sketch needs to establish what mock/emulation strategy makes these tests feasible.

## Worker Report

*(To be appended by the implementer after completion)*
