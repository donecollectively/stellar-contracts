# Post-Work Audit: tx-submit-error-handling

**Date**: 2026-02-28
**Work Unit**: `20260228.tx-submit-error-handling.workUnit.md`
**Commit under review**: `c8c7015` — `tx-submit machine: fixes implemented`
**Files in scope**: `src/networkClients/TxSubmitMgr.ts`, `tests/02c-TxSubmitMgr.test.ts`

## Reviewers

- Architect (archie)
- Code Whisperer (cwispy)
- Domain-Fit Tester — Stellar Testing (tessa)

---

## Findings

### Finding: f1 — Silent terminal failure when rescue budget exhausts

**Status**: resolved
**Reviewer**: Code Whisperer
**Severity**: Concern
**Location**: In `onEntry.failed` handler (bounded rescue else-branch) at `src/networkClients/TxSubmitMgr.ts:718`

**Observation**: When `isBadTx` is set and `rescueAttempts` exceeds `maxRescueAttempts`, the handler does nothing — no log, no event, no state change. The state machine silently stops in `failed` with no indication to consumers or operators that it has permanently given up. The `$$statusSummary` getter exposes `deferredAction` (which will be empty) and `isBadTx` (which is set), but there's no explicit "permanently failed" signal. In production debugging, the difference between "still trying" and "permanently given up" is invisible without inspecting `rescueAttempts` directly.

**Resolution**: Added `this.log()` call in the else branch: `"reached maximum rescue attempts for transaction (N/5)"`. All tests pass.

---

### Finding: f2 — Unreachable `resubmit` transition is dead code

**Status**: resolved — kept intentionally, future work to wire up
**Reviewer**: Code Whisperer
**Severity**: Concern
**Location**: In `TxSubmitterTransitions` union type, `noTransitionsExcept`, and `failed` transition table at `src/networkClients/TxSubmitMgr.ts:109,136,1003-1007`

**Observation**: The `resubmit` transition is added to the type union, the `noTransitionsExcept` guard object, and the `failed` state's transition table — but nothing in the codebase triggers it. The work unit plan originally included a `!successfulSubmitAt` branch in `onEntry.failed` that would fire `resubmit`, but analysis during implementation showed this branch is unreachable (the `confirming.notOk` handler redirects never-submitted txs back to `submitting` before they reach `failed`). The transition was left wired "for future use."

This is dead code with three touch points across the file. The test file includes a comment explaining the unreachability, but a reader of `TxSubmitMgr.ts` itself sees a wired transition with no caller and no in-file explanation of why.

**Resolution**: Transition retained intentionally. Future work will add detection logic to make all branches reachable and useful — the `resubmit` path from `failed → submitting` will be wired to cases where the tx was never successfully submitted but can be retried. This is follow-on implementation, not a post-work fix.

---

### Finding: f3 — `$$statusSummary` doesn't expose `rescueAttempts`

**Status**: open
**Reviewer**: Architect
**Severity**: Concern
**Location**: In `$$statusSummary` getter at `src/networkClients/TxSubmitMgr.ts:155-191`

**Observation**: The new `rescueAttempts` counter is added to `SubmitManagerState` and used in `onEntry.failed`, but it is not exposed through `$$statusSummary` — the primary interface through which `TxSubmissionTracker` and UI consumers observe submitter state. Consumers can see `isBadTx` and `status: "failed"` but cannot distinguish "rescue in progress (attempt 3/5)" from "rescue exhausted (permanently failed)." The `deferredAction` field provides indirect evidence (empty when exhausted) but is not a clear signal. This limits the upstream `updateSubmitterState` aggregation and any UI that renders submission progress.

**Resolution**:

---

### Finding: f4 — No requirements exist for the implemented behavioral changes

**Status**: resolved — deferred to separate session
**Reviewer**: Architect
**Severity**: Objection
**Location**: Work unit Scope section — `REQT Items: None yet`

**Observation**: Three concrete behavioral changes have been implemented and committed: (1) `doSubmit()` now rethrows after diagnostics, (2) `failed` state now has bounded rescue with firm backoff for `isBadTx` txs, (3) `resubmit` transition wired in table. No REQTs exist for the Network Clients component (ARCH-bhdkyrr6wb). The work unit acknowledged this as a Draft gap, but the implementation is now committed — behavioral changes exist without driving requirements. This inverts the requirements-first discipline: the code defines the behavior, and requirements (if ever created) would be retroactively documenting what was already built.

**Resolution**: Deferred to a separate requirements-gathering session based on the actual implementation. The tests currently express the behavioral expectations concretely. REQTs will be discovered from the implementation and tests rather than reverse-engineered during post-work review. Commit: b774fdb

