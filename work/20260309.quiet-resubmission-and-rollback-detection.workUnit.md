# Work Unit: 9mxjazdpkd

**Title**: Quiet Transaction Resubmission & Micro-Fork Rollback Detection
**Created**: 2026-03-09
**Source**: ad-hoc — operational observation of submitted transactions vanishing due to micro-forks

> **Required context**: Load [work-planner.SKILL.md](../../skillz/work-planner/work-planner.SKILL.md) for lifecycle protocol, team composition, and sign-off procedures before operating on this work unit.

## Scope

**Required Skills**: offchain dev, stellar testing
**ARCH Items**:
- ARCH-pt3qaa3zm4 (UtxoIndex) — primary: rollback detection, resubmission loop, `pendingTxHashes` lifecycle change, new `PendingTxEntry` fields
- ARCH-bhdkyrr6wb (Network Clients) — context: TxSubmitMgr/TxSubmissionTracker already handle per-submitter retry; this feature operates at a higher level in CachedUtxoIndex

**REQT Items (existing, modified to NEXT)**:
- REQT/58b9nzgcbj (Confirm Pending Transaction) — must no longer remove from `pendingTxHashes`; must store `confirmedInBlockHash`
- REQT/ddzcp753jr (Confirmation Depth Tracking) — `pendingTxHashes.delete` moves here, triggered at "likely" threshold
- REQT/b6h7km6h44 (Rollback Interaction) — implements `forkRecoveryCount` increment, `pendingTxHashes` re-add
- REQT/fn70x96nxm (Startup Recovery) — must also trigger resubmission for restored pending entries on page reload
- REQT/jqz2m497vx (PendingTxEntry Type) — adds `confirmedInBlockHash`, `forkRecoveryCount`, `lastResubmitAt` fields

**REQT Items (existing NEXT, implemented in scope)**:
- REQT/yasww6cqa4 (Chain Intersection Discovery) — via `blocks/{tip}/previous?count=100`, compare against stored blocks
- REQT/4j3rs4pyjt (Rollback Recovery) — scoped to micro-fork: resolve tx confirmations (re-anchor or revert), mark blocks
- REQT/2grpnzb2q0 (Resolve Pending Tx Confirmations) — two-path: re-anchor if tx found on winning fork, revert to pending if not. Uniform for all txs.
- REQT/epwp74mn8x (Roll Back Processing Cursor) — mark rolled-back blocks as `"rolled back"`, save canonical replacements as `"unprocessed"`
- REQT/j4198pv9ea (Re-initiate Sync) — normal sync processing handles canonical blocks on next pass
- REQT/dnr06r6ch5 (Rollback Event) — emit `chainRollback` event with depth

**REQT Items (new, created)**:
- REQT/cbpw1a6j8q (Quiet Transaction Resubmission) — top-level: proactive resubmission of unconfirmed/shallowly-confirmed txs
- REQT/qdrr0es7bm (Resubmission on Startup) — `resolvePendingState()` triggers resubmission on page reload
- REQT/z9d167q2mw (Resubmission Loop) — `resubmitStalePendingTxs()` called each sync cycle
- REQT/bq1nmd4c8x (Resubmission Scope) — eligibility criteria for resubmission candidates
- REQT/sg0pqr0dx7 (Resubmission Throttle) — at most once per 10s per tx via `lastResubmitAt`
- REQT/zyw6grz9t1 (Submission Path Abstraction) — `resubmitTx()` method for future multi-submitter routing
- REQT/zhgbnajdjg (Harmless Error Handling) — swallow expected errors, log unexpected
- REQT/xsvqyh5gwb (confirmedInBlockHash Field) — block hash for precise rollback matching
- REQT/k55zssabq2 (forkRecoveryCount Field) — diagnostic counter for fork recovery
- REQT/pgyqdvwn17 (pendingTxHashes Lifecycle) — retain until "likely" depth
- REQT/kfemj6eteg (Block-Discovered PendingTxEntry Creation) — `processTransactionForNewUtxos()` creates entries for block-discovered txs with CBOR from Blockfrost

## Problem / Context

### Observed Problem

A transaction that is successfully submitted to the Cardano network occasionally vanishes — it never appears on-chain. This is consistent with a short-lived micro-fork: the node that accepted the transaction was on a fork that lost the chain race. The transaction left the mempool (because it was "included" in a block) but the block was orphaned.

### Current Behavior

The existing system has no detection or recovery for this scenario:
- **TxSubmitMgr** confirms the tx via `getTx()` — which may reflect the losing fork's state
- **CachedUtxoIndex** never sees the tx in a canonical block during its block-walk
- Eventually the tx's deadline expires and the existing rollback lifecycle (REQT/a9y19g0pmr) rolls it back — but by then, the validity window has typically closed and the tx is irrecoverable

### Desired Behavior

Two coordinated behaviors address this:

1. **Quiet resubmission**: CachedUtxoIndex proactively resubmits any pending transaction (including those with shallow "provisional" confirmation) back to the network on each sync cycle. If the tx is already confirmed on the winning chain, the resubmit harmlessly fails. If the confirming block was orphaned, the resubmit gets the tx back into the mempool for inclusion in a future block. This runs independently of rollback detection — it's a standing resilience behavior that can get a tx into the next block faster than waiting for rollback detection.

2. **Micro-fork rollback detection**: CachedUtxoIndex detects when stored blocks are no longer on the canonical chain by comparing against Blockfrost's `blocks/{tip}/previous?count=100`. For rolled-back blocks, it resolves each affected tx's confirmation uniformly (self-submitted and block-discovered alike): if the tx appears in a canonical replacement block, it re-anchors the confirmation to the new block; if not, it reverts to `"pending"` status — making the tx eligible for resubmission. Rollback detection never deletes UTxOs, records, or spent markers directly; that cleanup only happens through the existing pending tx rollback lifecycle (REQT-1.11.3) when a tx genuinely fails to land.

### Key Design Decision: `pendingTxHashes` Until "Likely"

Currently, `confirmPendingTx()` removes the txHash from `pendingTxHashes` immediately upon first confirmation. This means a provisionally-confirmed tx drops out of all "pending" scope — including resubmission eligibility — at the exact moment when it's most vulnerable to micro-fork orphaning.

The fix: keep txs in `pendingTxHashes` until `confirmState` reaches `"likely"` (depth ≥ `provisionalDepth`, default 4 blocks). This means:
- `isPending()` returns truthy for provisionally-confirmed txs — correct, because they're not yet reliably confirmed
- The resubmission loop naturally covers them without special-case queries
- `updateConfirmationDepths()` performs the `pendingTxHashes.delete` when advancing to "likely"

### Future Compatibility

This design anticipates:
- **Ogmios as additional submitter**: `resubmitTx()` method abstracts the submission path; today it's `this.network.submitTx()`, tomorrow it routes through multiple submitters
- **Dred tx relay**: externally-received txs enter through `registerPendingTx` (or a lighter variant) and land in the same `pendingTxHashes` + Dexie table, automatically eligible for resubmission
- **Full chain rollback detection (REQT-1.12)**: this work unit implements the micro-fork-relevant subset; deeper rollback recovery can extend the same detection mechanism

### Known Limitation

A tx confirmed in an orphaned block at depth ≥ `provisionalDepth` (reached "likely" before the rollback) would have been removed from `pendingTxHashes` and would NOT be resubmitted by the quiet resubmission loop. Full recovery requires the rollback detection path to revert the confirmation (REQT/2grpnzb2q0). In practice, Cardano micro-forks resolve within 1-3 blocks, well within the provisional window. Deeper rollbacks are extraordinarily rare.

## Focus Files

- `src/networkClients/UtxoIndex/CachedUtxoIndex.ts` — primary: rollback detection, resubmission loop, `pendingTxHashes` lifecycle, `confirmPendingTx` change
- `src/networkClients/UtxoIndex/types/PendingTxEntry.ts` — new fields: `confirmedInBlockHash`, `forkRecoveryCount`, `lastResubmitAt`
- `src/networkClients/UtxoIndex/types/UtxoStoreGeneric.ts` — interface additions if needed
- `src/networkClients/UtxoIndex/DexieUtxoStore.ts` — any new store queries
- `src/networkClients/UtxoIndex/blockfrostTypes/BlockDetails.ts` — `previous_block` field already present; confirm response shape for `/previous` endpoint
- `src/networkClients/UtxoIndex/UtxoIndex.reqts.md` — requirements updates
- `src/networkClients/UtxoIndex/02f-PendingTx.test.ts` — test updates
- `src/networkClients/UtxoIndex/02g-BlockProcessing.test.ts` — test updates
- `src/ui/PendingTxTracker.tsx` — context: may want to show fork recovery indicator (out of scope, but awareness needed)

## Plan / Guidance

### Phase 1: PendingTxEntry Fields & pendingTxHashes Lifecycle

1. Add optional fields to `PendingTxEntry`: `confirmedInBlockHash?: string`, `forkRecoveryCount?: number`, `lastResubmitAt?: number`
2. Modify `confirmPendingTx()`:
   - Store `confirmedInBlockHash` from the block being processed
   - **Remove** the `pendingTxHashes.delete(txHash)` call
   - Keep `pendingTxMap.delete` and `cleanupPendingSpentUtxoIds` as-is (session-scoped, not needed for resubmission)
3. Modify `updateConfirmationDepths()`:
   - When advancing `confirmState` to `"likely"`: call `pendingTxHashes.delete(txHash)`
4. Verify `isPending()` behavior: it now returns truthy for provisionally-confirmed txs — confirm this is correct for all callers

### Phase 2: Quiet Resubmission

1. Add `private async resubmitTx(signedCborHex: string): Promise<void>` — decodes CBOR, calls `this.network.submitTx()`, swallows expected errors (UTxO-already-spent, expiry)
2. Add `private async resubmitStalePendingTxs(): Promise<void>`:
   - For each txHash in `pendingTxHashes`:
     - Load `PendingTxEntry` from store
     - Skip if `lastResubmitAt` was < 10s ago
     - Skip if validity window expired (`lastValidSlot < lastBlockSlot`)
     - Skip if deadline block has reached "likely" depth (same check as rollback trigger)
     - Call `resubmitTx(entry.signedTxCborHex)`
     - Update `entry.lastResubmitAt = Date.now()`, save to store
3. Call `resubmitStalePendingTxs()` from `checkForNewTxns()` — after block processing, before `updateConfirmationDepths()`
4. Also call from `resolvePendingState()` for page-reload recovery

### Phase 3: Rollback Detection

1. Add `private async detectRolledBackBlocks(): Promise<string[]>` (returns rolled-back block hashes):
   - Fetch `blocks/{lastBlockId}/previous?count=100`
   - Sort response by `height` ascending
   - For each canonical block: compare hash against `store.findBlockByHeight(height)`
   - If stored block exists AND hash differs → collect the stored block's hash as rolled-back
2. Call `detectRolledBackBlocks()` from `checkForNewTxns()` — before block processing (so we clean up before indexing new blocks)

### Phase 4: Rollback Execution

1. Add `private async executeBlockRollback(rolledBackBlockHashes: string[], canonicalBlocks: BlockDetailsType[]): Promise<void>`:
   - Fetch `blocks/{canonicalBlockHash}/txs/cbor` for each canonical replacement block → build a map of `txHash → { blockHash, blockHeight, blockSlot, cbor }`
   - Fetch `blocks/{rolledBackBlockHash}/txs` for each rolled-back block → collect all txHashes affected
   - For each affected txHash:
     - Load `PendingTxEntry` from store (all txs should have one — block processing creates them)
     - If txHash found in canonical tx map → **re-anchor**: update `confirmedInBlockHash`, `confirmedAtBlockHeight`, `confirmedAtSlot`, recalculate depth/`confirmState`
     - If txHash NOT in canonical tx map → **revert**: call `revertConfirmedPendingTx(entry)`
   - Mark each rolled-back block as `state: "rolled back"` via `store.saveBlock()`
   - Save each canonical replacement block as `state: "unprocessed"` for normal sync to reprocess
   - Emit `chainRollback` event
2. Add `private async revertConfirmedPendingTx(entry: PendingTxEntry): Promise<void>`:
   - Set `status = "pending"`, clear `confirmedAtBlockHeight`, `confirmedAtSlot`, `confirmedInBlockHash`, `confirmState`, reset `confirmationBlockDepth = 0`
   - Increment `forkRecoveryCount`
   - Save to store
   - Re-add to `pendingTxHashes`
   - Do NOT touch UTxOs or records — speculative state remains correct

**Note — block-discovered PendingTxEntries**: `processTransactionForNewUtxos()` MUST create a `PendingTxEntry` for every tx it processes (with `status: "confirmed"`, `confirmState` based on depth). CBOR is available from Blockfrost via `blocks/{hash}/txs/cbor` or `txs/{hash}/cbor`. This ensures all txs — self-submitted and block-discovered — have entries for uniform rollback resolution.

### Ordering in checkForNewTxns()

```
checkForNewTxns():
  1. fetchAndStoreNewBlocks()          // discover new blocks
  2. detectRolledBackBlocks()          // compare canonical vs stored  
     → executeBlockRollback() for each  // clean up rolled-back blocks
  3. syncIncremental/syncCatchup       // process unprocessed blocks (including canonical replacements)
  4. resubmitStalePendingTxs()         // resubmit pending + provisional txs
  5. updateConfirmationDepths()        // advance confirmState, remove from pendingTxHashes at "likely"
  6. executeSettledRollbacks()          // existing deadline-based rollback
```

## Acceptance Criteria

- [ ] Pending transactions are resubmitted to the network on each sync cycle when their validity window is still open
- [ ] Provisionally-confirmed transactions are resubmitted (kept in `pendingTxHashes` until "likely")
- [ ] Resubmission is throttled to at most once per 10 seconds per transaction
- [ ] Resubmission stops when the deadline block reaches "likely" depth or the tx is confirmed at "likely" depth
- [ ] Page reload triggers resubmission for any surviving pending entries
- [ ] Rolled-back blocks are detected by comparing stored block hashes against canonical chain from Blockfrost `blocks/{tip}/previous?count=100`
- [ ] Block-discovered txs get `PendingTxEntry` created during `processTransactionForNewUtxos()` with CBOR from Blockfrost
- [ ] Txs confirmed in rolled-back blocks that appear on the winning fork are re-anchored (confirmation metadata patched, no disruption)
- [ ] Txs confirmed in rolled-back blocks that do NOT appear on the winning fork are reverted to `"pending"` with `forkRecoveryCount` incremented
- [ ] Rollback resolution never deletes UTxOs, records, or spent markers — cleanup is exclusively through the pending tx rollback lifecycle (REQT-1.11.3)
- [ ] Reverted pending txs are automatically resubmitted on the next sync cycle
- [ ] `confirmedInBlockHash` is stored on `PendingTxEntry` for precise rollback matching
- [ ] `chainRollback` event is emitted when a rollback is detected
- [ ] Existing pending tx lifecycle (deadline rollback, confirmation depth tracking, purge) continues to function correctly
- [ ] `isPending()` returns truthy for provisionally-confirmed transactions (acceptable behavioral change)
- [ ] Harmless resubmission errors (UTxO spent, tx expired) are swallowed; unexpected errors are logged

## Sign-Offs

### Pre-Work
| Reviewer | Status | Date | Notes / References |
|----------|--------|------|--------------------|
| Architect | Approved | 2026-03-09 | F1 withdrawn after code review — initial sync bypasses `processTransactionForNewUtxos()`. Scope well-bounded, ARCH items appropriate, design aligns with architecture. |
| Code Whisperer | Approved | 2026-03-09 | F2 resolved — sensible defaults for block-discovered PendingTxEntry, no type changes needed. No remaining structural concerns. |
| Domain-Fit Tester (stellar-testing) | Approved | 2026-03-09 | Zero findings. Test sketch in Advisory Notes covers all 5 suites + 02f extension. |

### Post-Work
| Reviewer | Status | Date | Notes / References |
|----------|--------|------|--------------------|
| Architect | Pending | — | — |
| Code Whisperer | Pending | — | — |
| Domain-Fit Tester (stellar-testing) | Pending | — | — |

## Advisory Notes

### Design Discussion (2026-03-09)

Collaborative design session between Architect, Code Whisperer, and Coder exploring the problem space and converging on this approach. Key decisions:

1. **CachedUtxoIndex owns resubmission** — it has the signed CBOR, block-depth knowledge, and sync loop. TxBatcher/TxSubmitMgr handle initial submission; CachedUtxoIndex handles persistent lifecycle.

2. **Resubmission is independent of rollback detection** — it runs as a standing resilience behavior every sync cycle. This gets txs back into the mempool faster than waiting for rollback detection to trigger.

3. **Resubmit provisional txs too** — txs with `confirmState === "provisional"` are in the micro-fork danger zone. Keeping them in `pendingTxHashes` until "likely" depth naturally includes them in resubmission scope.

4. **`blocks/{tip}/previous?count=100` for detection** — single API call covers any realistic rollback depth. `blocks/{hash}/txs` for discovering which txs were in a rolled-back block.

5. **Uniform tx treatment** — all txs (self-submitted, block-discovered, future Dred-relayed) get `PendingTxEntry` records. Rollback resolution is uniform: re-anchor if found on winning fork, revert to pending if not. Rollback detection NEVER touches UTxOs/records/spent-markers — that cleanup only happens through the existing pending tx rollback lifecycle (REQT-1.11.3).

6. **Re-anchor as common path** — most micro-fork rollbacks simply move the tx from one block to another. Re-anchoring patches confirmation metadata without disrupting the user. Only txs genuinely lost from the chain get the revert-to-pending treatment.

7. **CBOR always available** — Blockfrost provides CBOR via `blocks/{hash}/txs/cbor` (batch) and `txs/{hash}/cbor` (individual), so all `PendingTxEntry` records can have `signedTxCborHex` regardless of origin.

8. **Future-compatible** — `resubmitTx()` abstracts submission path for future Ogmios/Dred multi-path routing. Dred-relayed txs enter the same pending lifecycle.

### Pre-Work Collaborative Review (2026-03-09)

#### Architect leads — findings

##### Finding F1: Block-discovered PendingTxEntry creation scope — NOT AN ISSUE

Initial concern: REQT/kfemj6eteg requires `processTransactionForNewUtxos()` to create a `PendingTxEntry` for every tx it processes — would this create massive storage overhead during initial sync or catchup?

After reading the code: **no**. Initial sync (`syncNow()` fresh-start path) fetches UTxOs directly via `network.getUtxos()` and indexes them through `indexUtxoFromTxInput()` — it never calls `processTransactionForNewUtxos()`. The tip block is saved as `"processed"`. So REQT/kfemj6eteg only fires during incremental sync cycles (typically 1–2 blocks) or catchup (bounded by capo-address activity during the offline gap, not all chain activity). Trivial overhead in practice.

> **Resolution**: Non-issue — withdrawn after code review. Initial sync path bypasses `processTransactionForNewUtxos()` entirely. (commit TBD)

#### Code Whisperer — sequenced follow-up

##### Finding F2: PendingTxEntry type stability for block-discovered entries

REQT/kfemj6eteg states that fields not available for block-discovered txs "MUST be optional on `PendingTxEntry` or filled with sensible defaults." This is a type-signature-level decision with consumer impact. Currently `PendingTxEntry` has mandatory fields (`description`, `id`, `batchDepth`, `txCborHex`) that are meaningful only for self-submitted txs.

> **Resolution**: Sensible defaults — no type changes, no consumer ripple. Block-discovered entries use:
> - `description`: `"tx discovered from on-chain data"`
> - `id`: txHash (same as PK — no TxDescription available)
> - `batchDepth`: `86` (sentinel — no real batch gets that deep, clearly identifies block-discovered origin)
> - `txCborHex`: signed CBOR from Blockfrost (same as `signedTxCborHex` — unsigned CBOR unavailable for block-discovered txs; distinction only matters for build path)
> - `deadlineSlot`: parsed from CBOR validity interval + graceBuffer (typically already expired for confirmed txs — if entry is ever reverted to "pending" during rollback, expired deadline immediately triggers cleanup via `checkPendingDeadlines()`)
>
> Commit: (below)

#### Domain-Fit Tester (Stellar Testing) — zero findings

Reviewed acceptance criteria coverage, testability of rollback detection (mock Blockfrost responses for `blocks/{tip}/previous`), re-anchor vs. revert path test decomposition, resubmission error handling mock strategy, and pendingTxHashes lifecycle boundary tests. No pre-work findings; test sketch below.

### Pre-Work Test Sketch

**Placement**: All new tests in `02g-BlockProcessing.test.ts` (mock-only, block-level mechanics) except startup-recovery-resubmission which extends the existing 02f `startup recovery` describe. All five suites use the established 02g mock infrastructure: `createTestIndex()`, `seedProcessedBlocks()`, `mockBlockfrost()`, `makeBlock()`, `advanceTipAndSync()`.

**New helper**: `seedPendingTx(index, txHash, overrides?)` — creates a `PendingTxEntry` in the store and adds to `pendingTxHashes` set. Reduces boilerplate across 4 of 5 suites.

#### Suite 1: pendingTxHashes Lifecycle (REQT/pgyqdvwn17)

```
describe "pendingTxHashes Lifecycle (REQT/pgyqdvwn17)"
  seedPendingTx + register in pendingTxHashes
  
  it "confirmPendingTx does NOT remove from pendingTxHashes (confirm-keeps-pending/REQT/pgyqdvwn17)"
    — mock block discovery w/ pending tx in block 101
    — after checkForNewTxns: entry.status === "confirmed", but pendingTxHashes.has(txHash) still true
    — isPending(txHash#0) still truthy
  
  it "updateConfirmationDepths removes at likely (remove-at-likely/REQT/pgyqdvwn17)"
    — setup confirmed entry at block 100
    — advanceTipAndSync past provisionalDepth (block 104)
    — pendingTxHashes.has(txHash) === false
    — isPending returns undefined for that txHash
  
  it "loadPendingFromStore populates pendingTxHashes on startup (startup-populates/REQT/pgyqdvwn17)"
    — seedPendingTx, close index, create index2 from same DB
    — call loadPendingFromStore
    — pendingTxHashes.has(txHash) === true
```

#### Suite 2: Quiet Resubmission (REQT/cbpw1a6j8q)

```
describe "Quiet Resubmission (REQT/cbpw1a6j8q)"
  seedPendingTx w/ signedTxCborHex + valid deadlineSlot
  mock network.submitTx → spy
  
  it "resubmits pending+provisional txs each sync cycle (resubmit-each-cycle/REQT/z9d167q2mw)"
    — seedPendingTx status:"pending" + another status:"confirmed" confirmState:"provisional"
    — run checkForNewTxns (which calls resubmitStalePendingTxs)
    — expect submitTx called twice (once per eligible tx)
  
  it "throttles to 10s per tx via lastResubmitAt (resubmit-throttle/REQT/sg0pqr0dx7)"
    — seedPendingTx, set lastResubmitAt = Date.now() (< 10s ago)
    — run resubmitStalePendingTxs
    — expect submitTx NOT called
    — set lastResubmitAt = Date.now() - 11000
    — run resubmitStalePendingTxs
    — expect submitTx called
  
  it "skips txs with expired validity window (skip-expired/REQT/bq1nmd4c8x)"
    — seedPendingTx w/ deadlineSlot < lastBlockSlot (expired)
    — resubmitStalePendingTxs
    — submitTx NOT called
  
  it "swallows harmless errors, logs unexpected (harmless-errors/REQT/zhgbnajdjg)"
    — mock submitTx → throw with "SubmissionUtxoError" (or message match)
    — resubmitStalePendingTxs completes without throwing
    — mock submitTx → throw with "UnexpectedError"
    — resubmitStalePendingTxs still completes, logError called
```

#### Suite 3: Chain Rollback Detection (REQT/jrhh4jg6se)

```
describe "Chain Rollback Detection (REQT/jrhh4jg6se)"
  seedProcessedBlocks 98..102
  
  it "detects rolled-back blocks via canonical chain comparison (detect-rollback/REQT/yasww6cqa4)"
    — mock blocks/{tip}/previous?count=100 → canonical 98-102 with block 101 hash DIFFERENT from stored
    — detectRolledBackBlocks returns [storedBlock101Hash]
  
  it "no rollback when all stored blocks match canonical (no-rollback/REQT/yasww6cqa4)"
    — mock blocks/{tip}/previous → all hashes match stored
    — detectRolledBackBlocks returns []
```

#### Suite 4: Rollback Execution (REQT/4j3rs4pyjt)

```
describe "Rollback Execution (REQT/4j3rs4pyjt)"
  seedProcessedBlocks + seedPendingTx confirmed in block 101 w/ confirmedInBlockHash
  
  it "re-anchors tx found on canonical fork (re-anchor/REQT/2grpnzb2q0)"
    — block 101 rolled back, canonical block 101' contains same txHash
    — mock blocks/{canonicalHash}/txs → [txHash]
    — executeBlockRollback
    — entry.confirmedInBlockHash === canonical101Hash
    — entry.status still "confirmed"
    — no forkRecoveryCount increment
  
  it "reverts tx not found on canonical fork (revert-to-pending/REQT/2grpnzb2q0)"
    — block 101 rolled back, canonical block 101' does NOT contain txHash
    — mock blocks/{canonicalHash}/txs → [] (empty)
    — executeBlockRollback
    — entry.status === "pending", confirmState === undefined
    — forkRecoveryCount === 1
    — pendingTxHashes.has(txHash) === true
  
  it "marks rolled-back blocks + saves canonical as unprocessed (block-state/REQT/epwp74mn8x)"
    — after executeBlockRollback
    — store.findBlockId(oldHash).state === "rolled back"
    — store.findBlockId(canonicalHash).state === "unprocessed"
  
  it "emits chainRollback event (rollback-event/REQT/dnr06r6ch5)"
    — listen for chainRollback event
    — executeBlockRollback
    — event emitted with depth info
  
  it "reverted pending tx resubmitted next cycle (revert-resubmit/REQT/z9d167q2mw)"
    — revert tx to pending (via rollback)
    — run checkForNewTxns again
    — submitTx called for the reverted tx
```

#### Suite 5: Block-Discovered PendingTxEntry (REQT/kfemj6eteg)

```
describe "Block-Discovered PendingTxEntry (REQT/kfemj6eteg)"
  
  it "processTransactionForNewUtxos creates entry for block txs (block-discovered-create/REQT/kfemj6eteg)"
    — mock a tx in block 101 that is NOT in pendingTxHashes
    — mock txs/{hash}/cbor → valid CBOR
    — let processTransactionForNewUtxos run (NOT mocked)
    — store.findPendingTx(txHash) exists with:
      description: "tx discovered from on-chain data"
      id: txHash, batchDepth: 86
      status: "confirmed", confirmState based on depth
  
  it "skips when PendingTxEntry already exists (skip-existing/REQT/kfemj6eteg)"
    — seedPendingTx (self-submitted entry exists)
    — process same txHash from a block
    — entry unchanged (description, batchDepth still original values)
```

#### Extension in 02f: Startup Recovery + Resubmission

```
// In 02f-PendingTx.test.ts, inside existing "startup recovery" describe:
it "resolvePendingState triggers resubmission for surviving entries (startup-resubmit/REQT/qdrr0es7bm)"
  — register pending tx in index1, create index2 from same DB
  — mock network.submitTx → spy
  — loadPendingFromStore + resolvePendingState on index2
  — expect submitTx called for the surviving pending entry
```

## Coder Acceptance — Clarifications (2026-03-09)

### Intention Alignment

The Coder's understanding of the problem was corrected: a transaction submitted to a micro-fork node *might* appear on the canonical chain or *might not* — it is not guaranteed to vanish. The two coordinated behaviors (quiet resubmission + rollback detection) address both outcomes.

### Questions & Resolutions

| # | Question | Resolution |
|---|----------|------------|
| 1 | **Block-discovered PendingTxEntry CBOR sourcing**: Should we use the existing per-tx `findOrFetchTxDetails` path (which fetches `txs/{txId}/cbor` individually) for block-discovered entries, or add a batch `blocks/{hash}/txs/cbor` call? | Use `txs/{txId}/cbor` via existing `findOrFetchTxDetails` path. Block-discovered txs from other users won't be cached in the store, but the per-tx endpoint serves the need. If `blocks/{hash}/txs/cbor` proves unnecessary, that's okay — don't add it speculatively. |
| 2 | **`lastValidSlot` for resubmission scope check**: Should we compare `deadlineSlot` directly against `lastBlockSlot`, or extract the raw validity end via `deadlineSlot - graceBufferSlots`? | Use `deadlineSlot - graceBufferSlots` to get the raw validity end for comparison. Note: `graceBufferSlots` defaults to 60 slots, not 4 — the "4" is `provisionalDepth` (blocks). The validity window check is slot-based. |
| 3 | **`loadPendingFromStore` for provisional txs on reload**: Should it also load `status === "confirmed"` entries where `confirmState === "provisional"` into `pendingTxHashes`? | Yes. On reload, provisionally-confirmed entries must be re-added to `pendingTxHashes` to maintain the lifecycle invariant that entries remain pending until "likely" depth. |

### Tasks Recognized

None — all work is within scope as specified.

## Worker Report

### Phase 1 Progress — PendingTxEntry Fields & pendingTxHashes Lifecycle
**Commit**: `8d6bd9f7`
**Status**: Complete

**Changes**:
- `types/PendingTxEntry.ts` — Added `confirmedInBlockHash?`, `forkRecoveryCount?`, `lastResubmitAt?` fields with full JSDoc
- `CachedUtxoIndex.ts` — `confirmPendingTx()`: stores `confirmedInBlockHash` from block lookup, **removes** the `pendingTxHashes.delete(txHash)` call
- `CachedUtxoIndex.ts` — `updateConfirmationDepths()`: adds `pendingTxHashes.delete(txHash)` when transitioning from `"provisional"` to any deeper state
- `CachedUtxoIndex.ts` — `loadPendingFromStore()`: also loads `status === "confirmed"` + `confirmState === "provisional"` entries into `pendingTxHashes` on startup
- `UtxoIndex.reqts.md` — All in-scope requirements transitioned NEXT → IN PROGRESS

**Behavioral change verified**: `isPending()` now returns truthy for provisionally-confirmed txs — correct per design, as they are not yet reliably confirmed.

**No interface changes needed**: `UtxoStoreGeneric` and `DexieUtxoStore` already support the needed operations. The new PendingTxEntry fields are optional and require no schema migration.

### Phase 2 Progress — Quiet Transaction Resubmission
**Commit**: `aeabebd5`
**Status**: Complete

**Changes**:
- `CachedUtxoIndex.ts` — Added `resubmitTx(signedCborHex)`: decodes CBOR, submits via `network.submitTx()`, swallows `SubmissionUtxoError` and `SubmissionExpiryError`, logs unexpected errors as warnings
- `CachedUtxoIndex.ts` — Added `resubmitStalePendingTxs()`: iterates `pendingTxHashes`, checks 4 eligibility criteria (status, validity window via `deadlineSlot - graceBufferSlots`, deadline depth, 10s throttle), calls `resubmitTx` for eligible entries, updates `lastResubmitAt`
- `CachedUtxoIndex.ts` — Wired into `checkForNewTxns()` after block processing, before `updateConfirmationDepths()`
- `CachedUtxoIndex.ts` — Wired into `resolvePendingState()` for startup recovery resubmission (before flipping to "fresh")

**Design note**: The resubmission loop position in `checkForNewTxns` is deliberately between block processing and depth advancement. This means txs that get reverted by rollback detection (Phase 3/4, when implemented above this point) will be immediately eligible for resubmission in the same sync cycle.

### Phase 3 Progress — Chain Rollback Detection & Recovery
**Commit**: `682246ee`
**Status**: Complete

**Changes**:
- `CachedUtxoIndex.ts` — Added `ChainRollbackEvent` interface and `chainRollback` event on `CachedUtxoIndexEvents` (REQT/dnr06r6ch5)
- `CachedUtxoIndex.ts` — Implemented `detectRolledBackBlocks()`: fetches `blocks/{tip}/previous?count=100`, compares canonical hashes against stored blocks via `store.findBlockByHeight()`, returns `{ rolledBackHashes, canonicalBlocks }` to avoid redundant API call in execution (REQT/yasww6cqa4)
- `CachedUtxoIndex.ts` — Implemented `executeBlockRollback()`: builds canonical tx set from replacement blocks, resolves each affected PendingTxEntry via re-anchor or revert path, marks rolled-back blocks as `"rolled back"`, saves canonical replacements as `"unprocessed"`, emits `chainRollback` event (REQT/4j3rs4pyjt, REQT/2grpnzb2q0, REQT/epwp74mn8x, REQT/dnr06r6ch5)
- `CachedUtxoIndex.ts` — Implemented `revertConfirmedPendingTx()`: clears confirmation fields, increments `forkRecoveryCount`, re-adds to `pendingTxHashes` for resubmission eligibility (REQT/b6h7km6h44, REQT/k55zssabq2)
- `CachedUtxoIndex.ts` — Wired `detectRolledBackBlocks()` → `executeBlockRollback()` into `checkForNewTxns()` after `fetchAndStoreNewBlocks()` but before sync processing (error-wrapped)
- `CachedUtxoIndex.ts` — Added block-discovered PendingTxEntry creation at end of `processTransactionForNewUtxos()`: creates entry with sensible defaults (`description: "tx discovered from on-chain data"`, `id: txHash`, `batchDepth: 86` sentinel, `deadlineSlot` from `tx.body.lastValidSlot`) for txs not already tracked (REQT/kfemj6eteg)
- `02g-BlockProcessing.test.ts` — Updated test destructuring for new `detectRolledBackBlocks()` return type; added `blocks/{tip}/previous` mock endpoints and `resubmitTx` mocks to tests that call `checkForNewTxns()` with dummy CBOR data

**Tests passing (9/9)**:
- Chain Rollback Detection: detect-rollback, no-rollback
- Rollback Execution: re-anchor, revert-to-pending, block-state, rollback-event, revert-resubmit
- Block-Discovered PendingTxEntry: block-discovered-create, skip-existing

**Design notes**:
- `detectRolledBackBlocks()` returns canonical blocks alongside hashes to avoid a second `previous?count=100` API call in `executeBlockRollback()`
- Re-anchor path preserves confirmation status with recalculated depth/confirmState from canonical block height
- Revert path clears all confirmation fields and re-adds to `pendingTxHashes` — the tx is then immediately eligible for resubmission in the same sync cycle (Phase 2 loop runs after rollback detection)
- Block-discovered entries use `batchDepth: 86` as a sentinel to distinguish from self-submitted entries

*(Phase 4 to follow)*
