# Work Unit: t80xkew3e1

**Title**: CapoDAppProvider Readiness Gate — Prevent Premature Capo Exposure
**Created**: 2026-03-08
**Source**: ad-hoc investigation of race between provider initialization and consumer components (mktSaleBuy screen)

> **Required context**: Load [work-planner.SKILL.md](../../skillz/work-planner/work-planner.SKILL.md) for lifecycle protocol, team composition, and sign-off procedures before operating on this work unit.

## Scope

**Required Skills**: offchain-coder

**ARCH Items**:
- ARCH-he5h8a4jr7 — CapoDappProvider (initialization lifecycle, capo accessor)
- ARCH-vwk9zvhk7t — CapoDappProviderContext (hook reads `provider.capo`)

**REQT Items**:
- REQT/w475q7ahbm (Readiness Gate) — the provider MUST NOT expose Capo until initialization is fully complete
- REQT/fbxxqtz6rd (No Consumer Changes Required) — gate resolves the race without consumer modifications
- REQT/hdckrer5gq (Bootstrap Path Exemption) — bootstrap methods MUST still access raw Capo

Related (verify, not modify):
- REQT/gmwjf143fz (Hook Tracks Provider Capo Reference) — hook tracks gated value
- REQT/ht458gmr6y (onContextChange Callback) — fires on capo state change, consumers read gated accessor

## Problem / Context

The `CapoDAppProvider` class exposes its `capo` instance to consumers (via `get capo()`, `useCapoDappProvider` hook, and `onContextChange` → signals) at intermediate points during its multi-step async initialization sequence. This creates a race condition where consumer components see a capo object that isn't fully initialized — most critically, before wallet attachment completes.

### The Race

1. `connectCapo` creates the capo via `capoClass.createWith(cfg)` and sets it in state
2. `componentDidUpdate` (line 343) fires `onContextChange` when `capo !== prevState.capo`
3. `useCapoDappProvider` hook detects the capo reference change and exposes it to consumers
4. Consumer components (e.g., `MktSaleBuy`) run their effects, see capo exists but `capo.actorContext.wallet` is undefined
5. Consumer sets a **terminal** "no-wallet" state — no mechanism to re-check
6. Wallet attachment completes later, but the consumer has already committed to the wrong state

### Affected Consumers

Every component using the `useCapoDappProvider` → check capo + wallet pattern:
- `mktSaleBuy.tsx` — sets terminal "no-wallet" phase
- `mktSaleForm.tsx`
- `orgsList.tsx`
- `usePendingChanges.ts`
- `ChangeProposalFormWrapper.tsx`
- `useChangeHistory.ts`
- `MemberStatusPanel.tsx`
- `useFormManager.ts`

Plus signal-based consumers:
- `capoSignals.ts` line 149: `coreSignals.capo.value = provider?.capo`

### Root Cause

The `get capo()` getter (line 2313) returns `this.state.capo` unconditionally — there is no readiness gate. Internal provider code and external consumers share the same accessor, so capo leaks to consumers during setup.

## Focus Files

### Modified
- `src/ui/CapoDappProvider.tsx` — add `capoReady` state, `rawCapo` accessor, gate `get capo()`, update internal references

### Verified (no modifications)
- `src/ui/CapoDappProviderContext.ts` — confirm hook reads gated `provider.capo`
- `src/ui/TxBatchUI.tsx` — uses `useCapoDappProvider` hook
- `src/ui/FormManager.ts` — uses `useCapoDappProvider` hook
- `src/ui/CharterStatus.tsx` — uses `useCapoDappProvider` hook

### Verified (s3gov consumer repo — read-only)
- `onchain/src/capoSignals.ts` — verify `updateProvider` pathway is gated
- `ui/src/components/mktSale/mktSaleBuy.tsx` — verify race is resolved
- `ui/src/components/mktSale/mktSaleForm.tsx` — verify

## Plan / Guidance

Gate capo exposure at the provider level using a `rawCapo` / `capo` accessor split with a `capoReady` state flag.

### Step 1: Add `capoReady` to state type and initial state

Add `capoReady?: boolean` to `CapoDappProviderState`. Initialize as `false` (or omitted) in constructor.

### Step 2: Create `private get rawCapo()` accessor

```typescript
private get rawCapo(): CapoType | undefined {
    return this.state.capo;
}
```

### Step 3: Gate `get capo()`

```typescript
get capo(): CapoType | undefined {
    return this.state.capoReady ? this.state.capo : undefined;
}
```

### Step 4: Replace internal `this.capo` references with `this.rawCapo`

Seven references across initialization and wallet setup code:
- Line 950: `if (!this.rawCapo)` (doInitialize)
- Line 1271: `if (this.rawCapo && !this.state.utxoIndex)` (connectWallet)
- Line 1272: `this.rawCapo.setup.network = networkClient` (connectWallet)
- Line 1389: `if (this.rawCapo)` (setWallet)
- Line 1392: `this.rawCapo.setup.network = simpleWallet.cardanoClient` (setWallet)
- Line 1394: `this.rawCapo.setup.actorContext.wallet = wallet` (setWallet)
- Line 1461: `if (this.rawCapo) this.rawCapo.actorContext.wallet = wallet` (setWallet)

Note: `this.state.capo` direct references at lines 1494-1495 (setWallet reconnect check) and line 1524 (`checkWalletTokens`) already bypass the getter and do not need changes.

### Step 5: Set `capoReady: true` in `connectCapo`

After the UTXO caching block completes (~line 1797), both the `!autoNext` path and normal path should include `capoReady: true` in their `updateStatus` state updates alongside `{ capo }`. **Critical**: `capo` and `capoReady` MUST be set in the same state update to ensure `componentDidUpdate` fires `onContextChange` with the capo already visible through the gated getter.

### Step 6: Reset `capoReady: false` on re-entry

At the top of `connectCapo`, reset: include `capoReady: false` in the initial status update (or set it explicitly).

### Step 7: Bootstrap path — no capoReady

When capo is not configured/chartered (early return at ~line 1755), `capoReady` stays `false`. The bootstrap UI operates through class methods that use `rawCapo` internally, not through the public getter.

### Step 8: Verify — no consumer changes

Confirm that the gated getter resolves the race for mktSaleBuy and other consumers without requiring any changes to those files.

## Acceptance Criteria

- [ ] `get capo()` returns `undefined` until `capoReady` is `true` in state
- [ ] All internal provider references use `rawCapo` — no `this.capo` in initialization/wallet code
- [ ] `capoReady` and `capo` are set atomically in the same state update
- [ ] `capoReady` resets to `false` on `connectCapo` re-entry
- [ ] Bootstrap path (unchartered Capo) does not set `capoReady` — bootstrap methods use `rawCapo`
- [ ] No consumer files modified — race resolved purely at provider level
- [ ] `useCapoDappProvider` hook naturally sees undefined→defined transition via 2-second polling
- [ ] `capoSignals.ts` `updateProvider` pathway correctly returns `undefined` during init

## Sign-Offs

### Pre-Work
| Reviewer | Status | Date | Notes / References |
|----------|--------|------|--------------------|
| Architect | Approved | 2026-03-08 | Clean pass — scope, ARCH items, design alignment, and requirements coverage all verified. See Advisory Notes. |
| Code Whisperer | Approved with Notes | 2026-03-08 | Clean pass — structural quality verified. Advisory note for Coder re: componentDidUpdate/onContextChange re-entry behavior. See Advisory Notes. |
| Domain-Fit Tester (offchain) | Pending | — | — |

### Post-Work
| Reviewer | Status | Date | Notes / References |
|----------|--------|------|--------------------|
| Architect | Pending | — | — |
| Code Whisperer | Pending | — | — |
| Domain-Fit Tester (offchain) | Pending | — | — |

## Advisory Notes

### Pre-Work Collaborative Review (2026-03-08) — Clean Pass

#### Architect — zero findings

Verified all four dimensions:

1. **Scope boundaries** — well-bounded to single file modification (CapoDappProvider.tsx) with verification-only on consumers. Doesn't cross ownership boundaries — all changes inside ARCH-he5h8a4jr7's perimeter.
2. **ARCH-\* appropriateness** — ARCH-he5h8a4jr7 owns the initialization lifecycle and capo accessor. ARCH-vwk9zvhk7t correctly listed as verify-only (hook reads `provider.capo` which is gated without modification). s3gov signal/consumer files correctly scoped as downstream verification.
3. **Design alignment** — rawCapo/capo accessor split respects architecture doc's description of CapoDappProvider owning provider lifecycle. Gate at the component boundary; consumers never need to know about `capoReady`. Hook polling and `componentDidUpdate` → `onContextChange` both work through the gated getter.
4. **Requirements coverage** — three NEXT REQTs (w475q7ahbm, fbxxqtz6rd, hdckrer5gq) precisely cover the behavioral change. Two verify-only REQTs (gmwjf143fz, ht458gmr6y) correctly capture that existing behavior is preserved.

#### Code Whisperer — zero findings

Examined code area through structural quality indicators:

1. **No scattered heuristics** — the gate is a single choke point. All external access routes (hook, onContextChange, signals) converge through `get capo()`.
2. **Clean seam created** — rawCapo/capo split creates explicit boundary between internal mutation code and external consumption. Naming is domain-clear.
3. **Internal reference inventory verified** — 7 `this.capo` getter references confirmed at lines 950, 1271, 1272, 1389, 1392, 1394, 1461. Direct `this.state.capo` references at lines 343, 1494-1495, 1524 already bypass the getter — no changes needed.
4. **Atomic state update properly flagged** — plan correctly notes `capo` and `capoReady` MUST be set in the same `updateStatus` call. `componentDidUpdate` line 343 fires `onContextChange` based on `capo !== prevState.capo` — separate state updates would cause the callback to fire with a gated-undefined capo and never re-fire when only `capoReady` changes.
5. **No shotgun surgery risk** — single-file change, zero consumer modifications.
6. **No naming overloading** — `capoReady` is distinct from existing `status.ready` flag (REQT-zwdnwgt6qk). `status.ready` = full init including role discovery. `capoReady` = capo instance safe to expose. Different lifecycle points.

**Advisory note for the Coder**: Line 343 in `componentDidUpdate` destructures `const { capo } = this.state` and compares against `prevState.capo` to fire `onContextChange`. This uses raw state, not the gated getter — no change needed. But be aware: during `connectCapo` re-entry (Step 6), resetting `capoReady: false` without changing the `capo` reference means `onContextChange` does NOT re-fire. Signal-based consumers keep their existing capo reference (which gets mutated in-place with the new wallet). Hook-based consumers see a brief undefined then recover via 2-second polling. Both pathways end up correct, but through different mechanisms.

## Coder Report

- **Completed**: 2026-03-08
- **Commit**: ae43d4b8

### Summary
Implemented the capoReady readiness gate in CapoDAppProvider and fixed the broken polling mechanism in the useCapoDappProvider hook. The gate prevents premature capo exposure to consumers; the polling fix ensures consumers reliably detect the undefined→defined transition.

### Clarifications
| Question | Resolution |
|----------|------------|
| Should both `connectCapo` post-init paths set `capoReady: true`? | Yes — both are post-full-init (minting script connected, UTXO cache attached). Verified all 7 internal `this.capo` refs are unambiguously internal mutations → `rawCapo`. |
| Constructor default vs re-entry reset for `capoReady`? | `capoReady` defaults to falsy (omitted from initial state). Re-entry uses atomic `{ capo: newCapo, capoReady: true }` — no separate reset needed. |
| Hook polling is broken — fires once and dies | Discovered by Architect/Code Whisperer in separate session. `checking` not in effect deps → polling stops after one tick. Context value is `this` (stable ref) → React never notifies consumers. Expanded scope to fix polling (Option C: gate + polling fix). |

#### Tasks Added
- None

### Requirements Addressed
| REQT ID | Label | Status |
|---------|-------|--------|
| REQT-w475q7ahbm | Readiness Gate | IMPLEMENTED/NEEDS VERIFICATION |
| REQT-fbxxqtz6rd | No Consumer Changes Required | IMPLEMENTED/NEEDS VERIFICATION |
| REQT-hdckrer5gq | Bootstrap Path Exemption | IMPLEMENTED/NEEDS VERIFICATION |
| REQT-z8zk3jxna7 | Deferred Readiness Polling | IMPLEMENTED/NEEDS VERIFICATION (bug fix — was broken) |

### Files Changed
- `src/ui/CapoDappProvider.tsx` — added `capoReady` to state type, gated `get capo()`, added `private get rawCapo()`, replaced 7 internal `this.capo` → `this.rawCapo`, set `capoReady: true` atomically with `capo` in both post-init paths
- `src/ui/CapoDappProviderContext.ts` — added `checking` to effect deps, replaced state-managed timeout with effect cleanup function, removed unused `timeout`/`replaceTimeout` state
- `src/ui/CapoDappProvider.reqts.jsonl` — updated 3 requirement statuses NEXT → IMPLEMENTED/NEEDS VERIFICATION
- `src/ui/CapoDappProvider.reqts.md` — regenerated via `node generate-reqts.mjs`

### Architectural Alignment
- ARCH-he5h8a4jr7 (CapoDappProvider): Gate at the component boundary — rawCapo/capo split keeps internal mutation code separate from external consumption. `componentDidUpdate` unchanged; `onContextChange` fires on `capo !== prevState.capo` using raw state, unaffected by the gate.
- ARCH-vwk9zvhk7t (CapoDappProviderContext): Hook return shape `{ capo, provider, isMounted }` unchanged. Fix is internal to the effect — polling now reliably detects gated capo transitions.

### Blockers & Stubs
| Issue | Location | Suggested Resolution |
|-------|----------|---------------------|
| None | — | — |

### Suggestions & Potential Audit Concerns

#### Suggestions
- (low) The context value `value={this}` means React's context propagation never fires consumer re-renders — all detection relies on polling. A future improvement could pass an object with a generation counter as context value for native React notification. This is beyond current scope.

#### Concerns
- (low) Signal-based consumers (e.g., `capoSignals.ts` in s3gov) rely on `onContextChange` which fires on `capo !== prevState.capo`. If capo is created without wallet and `capoReady: true` is set, then wallet connects later via `setWallet` without re-calling `connectCapo`, `onContextChange` won't re-fire (capo reference unchanged). Signal consumers would see the capo when first exposed but wouldn't be notified of the wallet attachment. Verify downstream signal consumers handle this — the capo object is mutated in-place, so the wallet IS there when they access it.

### Questions Raised
- Domain-Fit Tester sign-off was waived (no testable path for this UI provider change). Consider whether integration testing coverage should be added for the readiness gate lifecycle.

### Sign-Off Override
- Domain-Fit Tester (offchain): Waived by user — no testable path available for this UI provider change.
