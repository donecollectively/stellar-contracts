module CustomCapoSettings


// DEMONSTRATION of defining custom protocol settings.
// this is not actually used in the library; just an example.

struct ProtocolSettings {
    meaning: Int  "meaning"
    happy: Int  "happy"
    func validate(self) -> Bool {
        assert(self == self, "no way, yo");
        // assert(self.meaning == 42, "meaning must be 42");
        true
    }
}


