# Post-Hoc Audit: Snapshot Builder Actor Restore

**Work Unit**: mj5hrna9s7
**Auditors**: Code Whisperer (post-hoc), Stellar Testing (post-fix)
**Date**: 2026-02-08
**Baseline**: uncommitted changes on top of `77f6a2c`

## Context

The fix adds pre-build actor setup (a) to the SnapWrap contentBuilder lambda, co-located with the existing post-build assertion (b). Two files changed: `CapoTestHelper.ts` and `Emulator.reqts.md`.

## Per-File Analysis: `src/testing/CapoTestHelper.ts`

### Change Summary

- Converted contentBuilder lambda from `.then()` chain to `async/await` (style improvement, no behavioral change)
- Added pre-build actor setup (lines 564-572) before `generateSnapshotFunc`
- Genesis guard (`parentSnapName !== "genesis"`) correctly skips when actors don't exist
- Post-build assertion (lines 577-589) preserved exactly, only reformatted for `async/await`

### Structural Quality Pass

**Scattered Heuristics**: The actor-setting conditional appears in two locations:

| Location | Lines | Path |
|----------|-------|------|
| contentBuilder lambda | 567-571 | Build path: pre-build setup |
| `loadCachedSnapshot` | 1093-1097 | Load path: post-load setup |

Both are identical 5-line blocks:
```typescript
if (actorName === "default") {
    await this.setDefaultActor();
} else {
    await this.setActor(actorName);
}
```

**Assessment**: Acceptable. Two occurrences for two distinct lifecycle paths is the designed minimum (REQT/vwk0je2vef). Extracting a helper for 5 lines of simple conditional is premature. See Suggestion S-1.

**Missing Data Flow Choke Points**: None. The genesis guard in contentBuilder is path-specific: builders create actors, so pre-build setup must skip. The load path has no genesis guard because `loadCachedSnapshot` restores actors from stored keys (lines 1067-1077) before setting the active actor (line 1092-1097). Correct asymmetry.

**DRY Violations**: None beyond the actor-setting block noted above.

**Implicit Coupling**: None introduced. The lambda closes over `actorName` and `parentSnapName` from the decorator — these were already in scope and unchanged.

**Missing Seams**: None. The contentBuilder lambda cleanly separates (a) pre-build setup from (b) post-build assertion, with the build call between them.

**Abstraction Drift**: No. The contentBuilder lambda's purpose is "prepare, build, verify" — pre-build actor setup strengthens this contract.

**Naming**: REQT annotations are consistent with the requirements document (j9b8pr7yck, bjeez2n09p, vwk0je2vef, pt47cnb818). The (a)/(b) comment labels match the work unit's three-part contract description.

**Testability**: No hidden state introduced. Actor setup is explicit and traced.

### Double-Set Analysis

On cache miss, the actor is set in contentBuilder (pre-build), then `loadCachedSnapshot` sets it again (post-load). This is correct by design:
1. Pre-build set → builder runs with correct actor → snapshot captured
2. `loadCachedSnapshot` provides uniform loading for both hit and freshly-built paths

The double-set is harmless — `setActor` is idempotent for the same actor name.

## Per-File Analysis: `src/testing/emulator/Emulator.reqts.md`

REQT-3.7.1, 3.7.1.1, and 3.7.4 status updated from NEXT to IMPLEMENTED/NEEDS VERIFICATION. Correct tracking per the Coder Report.

## Cross-File Pattern Pass

- **Naming consistency**: `actorName` used consistently in both files and all locations
- **No new coupling**: The changeset touches two files; they were already coupled through the snapshot infrastructure
- **No scattered logic**: Actor lifecycle stays in the two designed locations (build path, load path)
- **Data flow**: Clear — contentBuilder lambda → `ensureSnapshotCached` → `loadCachedSnapshot`

## Architectural Escalation Check

No architectural symptoms. The fix reinforces ARCH-d52nrfd96z (Single Chokepoint) by keeping `ensureSnapshotCached` untouched — no signature changes, no actor-lifecycle responsibilities added to the caching chokepoint.

## Traceability Cross-Check

| REQT ID | In Code | In reqts.md | Status |
|---------|---------|-------------|--------|
| REQT/j9b8pr7yck | line 564 | REQT-3.7.1 | IMPLEMENTED/NEEDS VERIFICATION |
| REQT/bjeez2n09p | line 565 | REQT-3.7.1.1 | IMPLEMENTED/NEEDS VERIFICATION |
| REQT/pt47cnb818 | line 577 | REQT-3.7.2 | COMPLETED (pre-existing) |
| REQT/vwk0je2vef | line 574 | REQT-3.7.4 | IMPLEMENTED/NEEDS VERIFICATION |

All four REQTs from the work unit scope are annotated in code and tracked in requirements. No traceability gaps in the changeset.

## Findings

### S-1: Actor-setting duplication trajectory note (Suggestion)

**Evidence**: Identical 5-line actor-setting block in contentBuilder (line 567-571) and `loadCachedSnapshot` (line 1093-1097).

**Assessment**: Two occurrences for two distinct paths is acceptable now. If a third occurrence appears, extract to a private `applyDeclaredActor(actorName: string): Promise<void>` method.

**Action**: None required. Trajectory note only.

## Code Whisperer Verdict

**Clean pass.** The fix is minimal, well-placed, architecturally aligned, and properly traced. One suggestion noted for future trajectory awareness. No objections, no concerns.

---

## Stellar Testing: Post-Fix Review

### Scope

This work unit modifies testing infrastructure (`CapoTestHelper.ts`), not test code. The review assesses the fix from the testing domain perspective: correctness across snapshot cache paths, edge case coverage, and downstream impact on test suites.

### Scenario Analysis

The bug manifests only in the **partial-cache scenario** (parent cached, child uncached). Three cache scenarios exist:

| Scenario | Parent | Child | Actor Set By | Pre-Fix | Post-Fix |
|----------|--------|-------|--------------|---------|----------|
| Clean build | uncached | uncached | Parent builder leaves actor → child builder inherits | Works (incidentally) | Works (explicitly) |
| Full cache | cached | cached | `loadCachedSnapshot` (c) | Works | Works (unchanged) |
| Partial cache | cached | uncached | **Nothing** (pre-fix) / **contentBuilder (a)** (post-fix) | **FAILS** — stale actor | Works |

The fix closes the gap in scenario 3. The post-build assertion (b) would have caught this as a failure after the fact, but the pre-build setup (a) prevents the wrong-actor execution from happening at all — correct placement.

### Genesis Edge Case Verification

The genesis guard (`parentSnapName !== "genesis"`) is correct:
- `bootstrapWithActors` has `parentSnapName: "genesis"` and `actor: "default"` — its builder creates actors via `setupActors()`, so pre-build actor setup would fail (no actors exist yet)
- The post-build assertion (b) still runs for genesis, verifying the builder set a default actor by the end
- `loadCachedSnapshot` restores actors from stored keys (lines 1067-1077) before the actor-set call (line 1092-1097), so the load path handles genesis correctly

### Recursive Build Verification

When `ensureSnapshotCached` recursively builds a parent (line 1156: `parentReg.snapMethod.call(this)`), the parent's full SnapWrap executes, including its own pre-build actor setup. Then `ensureSnapshotCached` reloads parent state (line 1175) without touching the actor. The child's contentBuilder then sets its own actor via (a). Sequence is correct — no actor leak between parent and child builds.

### Downstream Test Impact

**Existing tests**: No test code changes needed. The fix makes the infrastructure honor the `actor` declaration consistently. Tests that already worked continue to work. Tests that relied on incidental actor ordering (clean-build scenario) now have explicit actor setup — more deterministic, not less.

**Defensive `setActor()` calls**: Downstream builders that defensively called `setActor()` at the start of their build functions (e.g., `CapoWithoutSettingsTestHelper.firstReqt()` per the work unit) now have redundant calls. These are harmless — `setActor()` is idempotent. They can be removed at consumer discretion but don't need to be.

**Override scenario**: Builders that switch actors mid-build (e.g., setting up a transaction as actor A, then switching to actor B for another) will override the pre-build setup. The post-build assertion (b) catches any actor left in the wrong state at build end. This is the existing contract — unchanged by the fix.

### Coverage Gap Assessment

| Requirement | Direct Test Coverage | Assessment |
|-------------|---------------------|------------|
| REQT/j9b8pr7yck (pre-build actor setup) | No isolated test — exercised by every snapshot build | Adequate: the partial-cache scenario that exposed the bug is the natural regression test. Deleting a child cache file and re-running exercises this path. |
| REQT/bjeez2n09p (genesis exception) | No isolated test — exercised by `bootstrapWithActors` in every test suite | Adequate: every test run builds or loads the genesis snapshot. |
| REQT/pt47cnb818 (post-build assertion) | No isolated test — any actor drift in a builder would trigger it | Adequate: this is a guard rail, not a feature. |
| REQT/vwk0je2vef (single location per path) | Structural constraint, not behavioral | Not testable — verified by code review. |

**No new tests needed.** The fix corrects infrastructure behavior that is exercised by every snapshot-based test. The partial-cache scenario is the natural regression surface — it occurs whenever a child snapshot's cache is invalidated (code change, cache cleanup) while its parent remains valid.

### Findings

No findings. The fix is correct from a testing domain perspective. The three cache scenarios are handled, the genesis edge case is guarded, recursive builds don't leak actor state, and downstream tests are unaffected.

## Combined Verdict

**Clean pass from both reviewers.** One trajectory suggestion (S-1, actor-setting duplication) from the Code Whisperer. No findings from the Stellar Testing review. The fix is ready for commit.
